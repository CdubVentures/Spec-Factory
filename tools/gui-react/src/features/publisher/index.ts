// Publisher frontend — public API.
// Owns UI primitives for configuring publisher-consumed field-rule knobs so
// the runtime consumer and the config UI stay colocated.

export { FormatPatternInput } from './components/FormatPatternInput.tsx';
