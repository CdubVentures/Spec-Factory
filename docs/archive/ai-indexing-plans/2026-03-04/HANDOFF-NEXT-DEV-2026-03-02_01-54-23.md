# Next Dev Handoff

Timestamp: 2026-03-02 01:54:23 (local)
Track: Pipeline settings full knob surfacing (Wave 3 reconciliation rollback + matrix sync)
Workspace: `C:\Users\Chris\Desktop\Spec Factory`

## 1) Current state

- Full regression gate is green on latest recheck:
  - `npm test` -> `3406 passed, 0 failed`
- GUI build was previously confirmed green in this session:
  - `npm --prefix tools/gui-react run build` passing
- Tracking artifacts are aligned:
  - `implementation/ai-indexing-plans/pipeline-knob-hierarchy-plan-2026-02-26.csv`
  - `implementation/ai-indexing-plans/pipeline-knob-matrix-2026-02-26_01-49-06.xlsx`
  - `implementation/ai-indexing-plans/WORK-PROGRESS-LEDGER.md`

## 2) What landed in this session

- Reverted prior alias reconciliation for two `not_implemented` rows after semantic mismatch review:
  - `Refresh TTL Window`
  - `Per-Round Rediscovery Cap`
- Restored both rows to `status_now=not_implemented` / `surface_now=not_implemented`.
- Re-synced matrix workbook tabs (`Knob Matrix`, `Main Group Counts`, `Sub Group Counts`, `Overview`).
- Logged rollback rationale in `WORK-PROGRESS-LEDGER.md` under:
  - `2026-03-02 - Wave 3 alias rollback (semantic mismatch guard)`

## 3) Current knob totals (from hierarchy CSV)

- Total audited knobs: `411`
- `gui`: `301`
- `backend_plus_gui`: `1`
- `backend_only`: `0`
- `hardcoded`: `97`
- `not_implemented`: `12`
- Remaining non-tunable backlog (`backend_only + hardcoded + not_implemented`): `109`
- Tunable now (`gui + backend_plus_gui`): `302`

## 4) Remaining explicit `not_implemented` queue (12)

1. `Refresh TTL Window`
2. `Per-Round Rediscovery Cap`
3. `WORKERS_SEARCH`
4. `WORKERS_FETCH`
5. `WORKERS_PARSE`
6. `WORKERS_LLM`
7. `WORKER_HEALTH_CHECK_INTERVAL_MS`
8. `WORKER_RESTART_BACKOFF_MS`
9. `429_BLOCK_RATE_THRESHOLD`
10. `MAX_BATCH_SIZE_CONFIRMATION`
11. `MAX_PARALLEL_PRODUCT_WORKERS`
12. `CHART_VISION_FALLBACK_ENABLED`

## 5) Next start plan

1. Reconfirm clean execution gates before new wiring:
   - `npm --prefix tools/gui-react run build`
   - `npm test`
2. Continue with the next validated surfacing slice from explicit `not_implemented` controls.
3. For each slice, enforce consistency across:
   - defaults (`src/shared/settingsDefaults.js`)
   - manifest/types (`tools/gui-react/src/stores/settingsManifest.ts`)
   - contract GET/PUT + validation (`src/api/services/settingsContract.js`)
   - runtime serializer/hydration (`tools/gui-react/src/stores/runtimeSettingsDomain.ts`, authority store)
   - runtime flow UI (`tools/gui-react/src/pages/pipeline-settings/RuntimeSettingsFlowCard.tsx`)
   - process env bridge (`src/api/routes/infraRoutes.js`)
   - focused tests
4. Refresh hierarchy + xlsx matrix after each completed slice.
5. Mandatory gate before any status update:
   - `npm test` must pass.

## 6) Constraints

- Worktree is intentionally dirty; do not reset/revert unrelated files.
- Keep changes scoped to knob surfacing slices and tracking artifacts.
- Preserve existing selector/contracts unless test updates are included in the same slice.
