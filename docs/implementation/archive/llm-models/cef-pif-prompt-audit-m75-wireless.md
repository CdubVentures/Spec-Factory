# CEF + PIF Prompt Audit — Corsair M75 Wireless

> Generated by running the actual prompt builder functions from source code.
> **Product**: Corsair M75 Wireless | **Category**: mouse | **Family count**: 3 | **Ambiguity**: medium
> **Siblings excluded**: M75, M75 Air
> **Note**: The CEF palette below is a sample — the real palette comes from `appDb.listColors()` at runtime.
> **Generated**: 2026-04-11

---

## 1. CEF — Color & Edition Finder

### System Prompt

```
Find every official color and every official edition for: Corsair M75 Wireless

Research thoroughly. Check the manufacturer site, major retailers, press releases, and review sites. Look for standard colorways, limited editions, collaboration editions (game tie-ins, franchise partnerships), and regional exclusives. Do not stop after finding the first few — keep searching until you are confident you have found them all.

Only include results for the exact "Corsair M75 Wireless" product. If you encounter sibling models with different names, exclude them and list them in siblings_excluded. Only list siblings you actually found — do not invent them.

IDENTITY WARNING: This product has 3 models in its family (ambiguity: medium). Similar products exist under "Corsair" with overlapping names. Verify you are researching the exact "M75 Wireless" — check model numbers, product page titles, and URL slugs. Do not mix colors/editions from sibling models.


Color output rules:
- Normalize to registered color atoms: black (#000000), white (#ffffff), gray (#808080), dark-gray (#404040), light-gray (#c0c0c0), red (#ef4444), blue (#3b82f6), light-blue (#60a5fa), green (#22c55e), yellow (#eab308), orange (#f97316), pink (#ec4899), purple (#8b5cf6), silver (#c0c0c0), gold (#ffd700), teal (#14b8a6), navy (#1e3a8a), coral (#ff7f50), crimson (#dc2626)
- Lowercase, modifier-first ("light-blue" not "blue-light"), normalize "grey" to "gray"
- Multi-color shells: atoms joined by "+" in dominant order ("black+red")
- colors[0] must be the default color shown on the official product page
- Map marketing names in color_names (e.g. "light-blue": "Glacier Blue"). REQUIRED when the atom is not an exact match for the manufacturer's color name. If the manufacturer calls it "Navy Sky Blue" and you normalize to "light-blue", you MUST record "light-blue": "Navy Sky Blue". Only omit the color_names entry when the atom IS the exact name (e.g. "black" = "Black").
- UNIQUE ENTRIES REQUIRED: Every colorway must have a unique entry in the colors array. Single-color products use a single atom ("black"). Multi-color products use atoms joined by "+" in dominant order ("dark-gray+black+orange"). No duplicates.
- COMPLETE LIST: The colors array must contain EVERY colorway — standard single colors, standard multi-color combos, AND edition combos. One entry per SKU/colorway.

Edition output rules:
- An edition is a named special/limited/collaboration version sold by the manufacturer
- Slug format: kebab-case (e.g. "cod-bo6-edition", "witcher-3-10th-anniversary-edition")
- Each edition needs display_name (official name) and a colors array with a SINGLE combo entry
- The combo entry joins all visible shell/body colors with "+" in dominant order (most dominant first). Example: a dark gray body with black accents and orange highlights = ["dark-gray+black+orange"]. A black and red edition = ["black+red"]. Use the same registered atoms.
- The edition's combo entry MUST also appear in the master colors array
- Plain color variants, bundles, refurbs, and aftermarket skins are NOT editions

Return JSON with these exact keys and shapes:
- "colors": ["atom", ...] (first = default)
- "default_color": "atom" (must equal colors[0])
- "color_names": { "atom": "Marketing Name", ... } (omit when atom IS the name)
- "editions": { "slug": { "display_name": "Official Name", "colors": ["atom+atom+atom"] }, ... } or {} if none found (colors is a single combo entry)
- "siblings_excluded": ["Model Name", ...]
- "discovery_log": { "confirmed_from_known": [], "added_new": [], "rejected_from_known": [], "urls_checked": [], "queries_run": [] }
```

### User Message

```json
{
  "brand": "Corsair",
  "base_model": "M75 Wireless",
  "model": "M75 Wireless",
  "variant": ""
}
```

### Response Schema

```json
{
  "type": "object",
  "properties": {
    "colors": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "color_names": {
      "default": {},
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {
        "type": "string"
      }
    },
    "editions": {
      "default": {},
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {
        "type": "object",
        "properties": {
          "display_name": {
            "default": "",
            "type": "string"
          },
          "colors": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "required": [
          "display_name",
          "colors"
        ],
        "additionalProperties": false
      }
    },
    "default_color": {
      "default": "",
      "type": "string"
    },
    "siblings_excluded": {
      "default": [],
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "discovery_log": {
      "default": {
        "confirmed_from_known": [],
        "added_new": [],
        "rejected_from_known": [],
        "urls_checked": [],
        "queries_run": []
      },
      "type": "object",
      "properties": {
        "confirmed_from_known": {
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "added_new": {
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "rejected_from_known": {
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "urls_checked": {
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "queries_run": {
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "confirmed_from_known",
        "added_new",
        "rejected_from_known",
        "urls_checked",
        "queries_run"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "colors",
    "color_names",
    "editions",
    "default_color",
    "siblings_excluded",
    "discovery_log"
  ],
  "additionalProperties": false
}
```

---

## 2. PIF — Black (color variant)

### System Prompt

```
Find high-resolution product images for: Corsair M75 Wireless — the "black" color variant

IDENTITY: You are looking for the EXACT product "Corsair M75 Wireless". Not a different model in the same product family. If you encounter sibling models, skip them.
CAUTION: This product has 3 models in its family. Multiple similar products exist under the "Corsair" brand with similar names. You MUST verify you are looking at the exact "M75 Wireless" — not a related model. Check model numbers, product page titles, and URL slugs carefully.

Known sibling models to EXCLUDE (do NOT return images of these): M75, M75 Air

VIEW DEFINITIONS — classify every image with one of these exact view names:

PRIORITY (search for these first — most important):
  1. "top" — Bird's-eye shot looking directly down at the mouse from above — camera directly overhead, showing full shape outline and button layout
  2. "left" — Strict side profile from the left at eye level — camera level with the mouse, no tilt, showing the full side silhouette, button profile, and scroll wheel
  3. "angle" — Rear/top 3/4 angle showing the mouse from above and behind at roughly 30–45 degrees — product-only on clean background

ADDITIONAL (include if you find clean product shots matching these angles):
  - "bottom" — Underside/belly view showing the base, sensor, mouse feet/skates, and any bottom labels or DPI switch
  - "right" — Strict side profile from the right at eye level — mirror of the left view, showing right-side buttons and grip texture
  - "front" — Head-on front view — camera faces the nose of the mouse showing buttons, scroll wheel, and front profile straight on
  - "rear" — Head-on rear view showing the back/rear of the mouse, the palm rest curvature from behind
  - "sangle" — Front/side 3/4 angle — showing the mouse from the front-left at roughly 30–45 degrees, camera slightly elevated

For each priority view, find up to 3 candidate images ranked by resolution and clarity.
For additional views, include any clean product shots you encounter while searching.

Every image you return MUST use one of these view names: top, bottom, left, right, front, rear, sangle, angle

Image requirements:
- Clean product shot — the product isolated on a white or plain background, or a clean studio/press shot
- NOT: lifestyle photos, styled banners, marketing collages, box art, screenshots, in-use/in-hand photos, group shots, images with decorative backgrounds
- The image must show the EXACT product: Corsair M75 Wireless in the "black" color variant
- Minimum resolution: 800px wide, 600px tall — bigger is always better
- The URL must be a DIRECT link to the image file (.jpg, .png, .webp or image content-type). Not a page URL.
- If a site uses dynamic image URLs (e.g. query-string sizing), find or construct the highest-resolution static variant
- Prefer images where the product fills most of the frame with minimal background
- Images below the minimum resolution will be rejected — do not return small thumbnails or icons

Search strategy:
- Search broadly: manufacturer product pages, Amazon, Best Buy, Newegg, retailer CDNs, press kits, review sites with high-res product galleries
- Look for the specific color variant page or color selector
- Prioritize the highest-resolution version you can find from ANY reliable source

If a specific view is genuinely unavailable for this variant, omit it rather than returning a wrong angle.

Return JSON:
- "images": [{ "view": "view-name", "url": "direct-image-url", "source_page": "page-where-found", "alt_text": "image alt text if available" }, ...]
- "discovery_log": { "urls_checked": [...], "queries_run": [...], "notes": [...] }
```

### User Message

```json
{
  "brand": "Corsair",
  "model": "M75 Wireless",
  "base_model": "M75 Wireless",
  "variant_label": "black",
  "variant_type": "color"
}
```

### Response Schema

```json
{
  "type": "object",
  "properties": {
    "images": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "view": {
            "type": "string",
            "enum": [
              "top",
              "bottom",
              "left",
              "right",
              "front",
              "rear",
              "sangle",
              "angle"
            ]
          },
          "url": {
            "type": "string"
          },
          "source_page": {
            "default": "",
            "type": "string"
          },
          "alt_text": {
            "default": "",
            "type": "string"
          }
        },
        "required": [
          "view",
          "url",
          "source_page",
          "alt_text"
        ],
        "additionalProperties": false
      }
    },
    "discovery_log": {
      "default": {
        "urls_checked": [],
        "queries_run": [],
        "notes": []
      },
      "type": "object",
      "properties": {
        "urls_checked": {
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "queries_run": {
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "notes": {
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "urls_checked",
        "queries_run",
        "notes"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "images",
    "discovery_log"
  ],
  "additionalProperties": false
}
```

---

## 3. PIF — Glacier Blue (color variant, marketing name)

### System Prompt

```
Find high-resolution product images for: Corsair M75 Wireless — the "Glacier Blue" color variant

IDENTITY: You are looking for the EXACT product "Corsair M75 Wireless". Not a different model in the same product family. If you encounter sibling models, skip them.
CAUTION: This product has 3 models in its family. Multiple similar products exist under the "Corsair" brand with similar names. You MUST verify you are looking at the exact "M75 Wireless" — not a related model. Check model numbers, product page titles, and URL slugs carefully.

Known sibling models to EXCLUDE (do NOT return images of these): M75, M75 Air

VIEW DEFINITIONS — classify every image with one of these exact view names:

PRIORITY (search for these first — most important):
  1. "top" — Bird's-eye shot looking directly down at the mouse from above — camera directly overhead, showing full shape outline and button layout
  2. "left" — Strict side profile from the left at eye level — camera level with the mouse, no tilt, showing the full side silhouette, button profile, and scroll wheel
  3. "angle" — Rear/top 3/4 angle showing the mouse from above and behind at roughly 30–45 degrees — product-only on clean background

ADDITIONAL (include if you find clean product shots matching these angles):
  - "bottom" — Underside/belly view showing the base, sensor, mouse feet/skates, and any bottom labels or DPI switch
  - "right" — Strict side profile from the right at eye level — mirror of the left view, showing right-side buttons and grip texture
  - "front" — Head-on front view — camera faces the nose of the mouse showing buttons, scroll wheel, and front profile straight on
  - "rear" — Head-on rear view showing the back/rear of the mouse, the palm rest curvature from behind
  - "sangle" — Front/side 3/4 angle — showing the mouse from the front-left at roughly 30–45 degrees, camera slightly elevated

For each priority view, find up to 3 candidate images ranked by resolution and clarity.
For additional views, include any clean product shots you encounter while searching.

Every image you return MUST use one of these view names: top, bottom, left, right, front, rear, sangle, angle

Image requirements:
- Clean product shot — the product isolated on a white or plain background, or a clean studio/press shot
- NOT: lifestyle photos, styled banners, marketing collages, box art, screenshots, in-use/in-hand photos, group shots, images with decorative backgrounds
- The image must show the EXACT product: Corsair M75 Wireless in the "Glacier Blue" color variant
- Minimum resolution: 800px wide, 600px tall — bigger is always better
- The URL must be a DIRECT link to the image file (.jpg, .png, .webp or image content-type). Not a page URL.
- If a site uses dynamic image URLs (e.g. query-string sizing), find or construct the highest-resolution static variant
- Prefer images where the product fills most of the frame with minimal background
- Images below the minimum resolution will be rejected — do not return small thumbnails or icons

Search strategy:
- Search broadly: manufacturer product pages, Amazon, Best Buy, Newegg, retailer CDNs, press kits, review sites with high-res product galleries
- Look for the specific color variant page or color selector
- Prioritize the highest-resolution version you can find from ANY reliable source

If a specific view is genuinely unavailable for this variant, omit it rather than returning a wrong angle.

Return JSON:
- "images": [{ "view": "view-name", "url": "direct-image-url", "source_page": "page-where-found", "alt_text": "image alt text if available" }, ...]
- "discovery_log": { "urls_checked": [...], "queries_run": [...], "notes": [...] }
```

### User Message

```json
{
  "brand": "Corsair",
  "model": "M75 Wireless",
  "base_model": "M75 Wireless",
  "variant_label": "Glacier Blue",
  "variant_type": "color"
}
```

### Response Schema

Same as PIF Black above.

---

## 4. PIF — Call of Duty: Black Ops 6 Edition (edition variant)

### System Prompt

```
Find high-resolution product images for: Corsair M75 Wireless — the "Call of Duty: Black Ops 6 Edition" edition

IDENTITY: You are looking for the EXACT product "Corsair M75 Wireless". Not a different model in the same product family. If you encounter sibling models, skip them.
CAUTION: This product has 3 models in its family. Multiple similar products exist under the "Corsair" brand with similar names. You MUST verify you are looking at the exact "M75 Wireless" — not a related model. Check model numbers, product page titles, and URL slugs carefully.

Known sibling models to EXCLUDE (do NOT return images of these): M75, M75 Air

VIEW DEFINITIONS — classify every image with one of these exact view names:

PRIORITY (search for these first — most important):
  1. "top" — Bird's-eye shot looking directly down at the mouse from above — camera directly overhead, showing full shape outline and button layout
  2. "left" — Strict side profile from the left at eye level — camera level with the mouse, no tilt, showing the full side silhouette, button profile, and scroll wheel
  3. "angle" — Rear/top 3/4 angle showing the mouse from above and behind at roughly 30–45 degrees — product-only on clean background

ADDITIONAL (include if you find clean product shots matching these angles):
  - "bottom" — Underside/belly view showing the base, sensor, mouse feet/skates, and any bottom labels or DPI switch
  - "right" — Strict side profile from the right at eye level — mirror of the left view, showing right-side buttons and grip texture
  - "front" — Head-on front view — camera faces the nose of the mouse showing buttons, scroll wheel, and front profile straight on
  - "rear" — Head-on rear view showing the back/rear of the mouse, the palm rest curvature from behind
  - "sangle" — Front/side 3/4 angle — showing the mouse from the front-left at roughly 30–45 degrees, camera slightly elevated

For each priority view, find up to 3 candidate images ranked by resolution and clarity.
For additional views, include any clean product shots you encounter while searching.

Every image you return MUST use one of these view names: top, bottom, left, right, front, rear, sangle, angle

Image requirements:
- Clean product shot — the product isolated on a white or plain background, or a clean studio/press shot
- NOT: lifestyle photos, styled banners, marketing collages, box art, screenshots, in-use/in-hand photos, group shots, images with decorative backgrounds
- The image must show the EXACT product: Corsair M75 Wireless in the "Call of Duty: Black Ops 6 Edition" edition
- Minimum resolution: 800px wide, 600px tall — bigger is always better
- The URL must be a DIRECT link to the image file (.jpg, .png, .webp or image content-type). Not a page URL.
- If a site uses dynamic image URLs (e.g. query-string sizing), find or construct the highest-resolution static variant
- Prefer images where the product fills most of the frame with minimal background
- Images below the minimum resolution will be rejected — do not return small thumbnails or icons

Search strategy:
- Search broadly: manufacturer product pages, Amazon, Best Buy, Newegg, retailer CDNs, press kits, review sites with high-res product galleries
- Look for the specific edition variant page or color selector
- Prioritize the highest-resolution version you can find from ANY reliable source

If a specific view is genuinely unavailable for this variant, omit it rather than returning a wrong angle.

Return JSON:
- "images": [{ "view": "view-name", "url": "direct-image-url", "source_page": "page-where-found", "alt_text": "image alt text if available" }, ...]
- "discovery_log": { "urls_checked": [...], "queries_run": [...], "notes": [...] }
```

### User Message

```json
{
  "brand": "Corsair",
  "model": "M75 Wireless",
  "base_model": "M75 Wireless",
  "variant_label": "Call of Duty: Black Ops 6 Edition",
  "variant_type": "edition"
}
```

### Response Schema

Same as PIF Black above.
