# Product ID Decoupling Audit — 2026-03-29

> **Problem:** `productId` is built from `slug(brand)-slug(model)-slug(variant)`. Renaming any identity field changes the productId, orphaning all data (SQL rows, storage keys, disk paths). The catalog already has a stable `identifier` (8-char hex) on every product — it just isn't used as the canonical key.

## The existing identifier system

Every catalog product already has:
```json
"mouse-alienware-aw610m": {        ← slug-based key (BREAKS on rename)
  "id": 1,                          ← numeric auto-increment
  "identifier": "121cc826",         ← 8-char hex, stable, immutable
  "brand": "Alienware",
  "model": "AW610M"
}
```

Brands also have identifiers. Generated by `generateIdentifier()` in `src/features/catalog/identity/productIdentity.js` using `crypto.randomBytes(4)`.

## Target state

`productId` becomes `{category}-{identifier}` (e.g. `mouse-121cc826`). Identity fields (brand, model, variant) are mutable metadata. The slug-based key becomes a display label only.

---

## Blast radius

### SQL tables with product_id: 28

| Table | Constraint | Index |
|-------|-----------|-------|
| `products` | UNIQUE(category, product_id) | — |
| `product_runs` | UNIQUE(category, product_id, run_id) | idx_pr_product |
| `product_queue` | UNIQUE(category, product_id) | idx_pq_product |
| `item_field_state` | UNIQUE(category, product_id, field_key) | idx_ifs_product |
| `item_component_links` | UNIQUE(category, product_id, field_key) | idx_icl_product |
| `item_list_links` | UNIQUE(category, product_id, field_key, list_value_id) | idx_ill_product |
| `candidates` | — | idx_cand_product_field |
| `crawl_sources` | COMPOSITE PK (content_hash, product_id) | idx_cs_product |
| `source_screenshots` | — | idx_ss_product |
| `source_videos` | — | idx_sv_product |
| `source_pdfs` | — | idx_sp_product |
| `field_history` | UNIQUE(category, product_id, field_key) | idx_fh_product |
| `runs` | — | idx_runs_product |
| `run_artifacts` | — | — |
| `bridge_events` | — | — |
| `runtime_events` | — | idx_re_product |
| `billing_entries` | — | idx_be_product |
| `evidence_documents` | — | idx_ed_category_product |
| `source_registry` | — | — |
| `curation_suggestions` | — | — |
| `component_review_queue` | — | — |
| `learning_profiles` | — | — |
| `query_index` | — | — |
| `url_index` | — | — |
| `prompt_index` | — | — |
| `knob_snapshots` | — | — |
| `audit_log` | — | — |
| `llm_route_matrix` | — | — |

### productId generation sites: 7

| File | Line | Context | Pattern |
|------|------|---------|---------|
| `src/app/cli/commands/pipelineCommands.js` | 79 | indexlab CLI | slug(brand)-slug(model)-slug(variant)-indexlab-timestamp |
| `src/app/cli/commands/pipelineCommands.js` | 281 | run-ad-hoc CLI | slug(brand)-slug(model)-slug(variant) |
| `src/app/cli/commands/queueCommand.js` | 29 | queue add CLI | slug(brand)-slug(model)-slug(variant) |
| `src/app/cli/commands/explainUnkCommand.js` | 11 | explain-unk CLI | slug(brand)-slug(model)-slug(variant) |
| `src/ingest/csvIngestor.js` | 105 | CSV batch ingest | slug(category)-slug(brand)-slug(model)-slug(variant) |
| `src/features/catalog/products/productCatalog.js` | 110 | addProduct (GUI) | via normalizeProductIdentity → slugify.buildProductId |
| `src/features/catalog/products/productCatalog.js` | 251 | addProductsBulk | same chain |

### Storage key patterns: 157 usage lines, 12 distinct patterns

- `resolveOutputKey(category, productId, 'latest')` — most common
- `resolveOutputKey(category, productId, 'final')`
- `resolveOutputKey(category, productId, 'review')`
- `resolveOutputKey(category, productId, 'runs', runId)`
- `resolveInputKey(category, 'products', productId + '.json')`

### Identity-based paths (NOT using productId): 1 HIGH-RISK

- `src/publish/driftScheduler.js:184` — `finalPathPartsFromIdentity()` builds `final/{cat}/{brand}/{model}/{variant}/`

### Test fixtures with hardcoded productIds: 57 files, 186 occurrences

Common patterns: `mouse-razer-viper-v3-pro`, `monitor-dell-u2724d`, `keyboard-corsair-k65-pro`

### GUI references: 48 TypeScript files, 108 occurrences

Internal routing and API payloads. GUI displays brand+model (human-readable), not raw productId.

### Catalog JSON: productId IS the object key

```json
{ "products": { "mouse-alienware-aw610m": { ... } } }
```

Changing productId means rebuilding the catalog object.

---

## Migration strategy

### Phase 1: New ID generator — COMPLETE
- `buildProductId(category)` → `{category}-{8-char-hex}` in `src/shared/primitives.js`
- Uses existing `crypto.randomBytes(4)` pattern from `generateIdentifier()`

### Phase 2: Update generation sites — COMPLETE (9 sites migrated)
- All sites switch to `buildProductId(category)`
- Explicit `--product-id` override still respected

### Phase 3: Catalog key migration — COMPLETE
- `productCatalog.js` uses `{category}-{identifier}` as the catalog key
- `normalizeProductIdentity` stops generating productId
- `updateProduct` identity changes do NOT change productId

### Phase 4: Fix driftScheduler — COMPLETE
- `finalPathPartsFromIdentity` → uses productId instead of identity slugs

### Phase 5: One-time data migration — COMPLETE
- `idFormatMigration.js` built + CLI wired: `spec migrate-product-ids --category mouse [--dry-run]`
- Rekeys 26 SQL tables in transaction
- `artifactMigration.js` moves 7 artifact prefix groups
- Data verified: mouse=343 products (all hex), monitor=0, keyboard=0

### Phase 6: Test fixture updates — COSMETIC DEBT
- Slug-based IDs remain in some test files (tests pass regardless)
- Phased cleanup only — not blocking

---

## Existing infrastructure that helps

- `generateIdentifier()` — already generates 8-char hex IDs
- `identifier` field — already on every product and brand in catalog
- `migrateProductArtifacts()` — already renames storage key prefixes
- `appendRenameLog()` — already logs renames
- `artifactMigration.js` — tested, handles latest/runs/final/published/review paths

## Delivered artifacts

- `buildProductId(category)` in `src/shared/primitives.js`
- `idFormatMigration.js` in `src/features/catalog/migrations/` — batch UPDATE for 26 tables
- `artifactMigration.js` — storage key relocation (7 prefix groups)
- CLI: `spec migrate-product-ids --category <cat> [--dry-run]`
- `HEX_ID_RE` guard in `productIdentityAuthority.js`

---

## Risk assessment

| Risk | Severity | Mitigation |
|------|----------|------------|
| Data loss during migration | HIGH | Run migration with dry-run first. Backup sqlite + catalog JSON before. |
| Test breakage (57 files) | MEDIUM | Phase 6 after all code changes. Factory pattern for future tests. |
| GUI disruption | LOW | GUI displays brand+model, not raw productId. Internal routing adapts. |
| Incomplete SQL migration | HIGH | Transaction-wrap all 28 table UPDATEs. Rollback on failure. |
| Old-format IDs in the wild | MEDIUM | Existing `--product-id` override still accepts old format for backward compat. |
