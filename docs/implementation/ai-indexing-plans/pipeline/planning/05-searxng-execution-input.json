{
  "_doc": "Final query payload that hits SearXNG via runSearchProviders(). Identical regardless of Schema 4 or old path origin.",

  "queries": ["string — the actual search strings sent to SearXNG"],

  "per_query_metadata": {
    "resolved_from": "selectedQueryRowMap.get(query.toLowerCase()) or profileQueryRowMap.get(query.toLowerCase())",
    "target_fields": ["string — fields this query aims to resolve (used for post-triage field attribution)"],
    "domain_hint": "string — preferred domain (used for site: prefix by some providers)",
    "doc_hint": "string — expected document type (spec_sheet, review, manual)",
    "hint_source": "string — origin of the hint: schema4_search_plan | targeted | host_plan | base_template",
    "family": "string — query family (manufacturer_html, manual_pdf, etc.)"
  },

  "execution_config": {
    "executionQueryLimit": "number — max queries to attempt",
    "queryConcurrency": "number — parallel query slots (default 1)",
    "resultsPerQuery": "number — max results per query (default 10)",
    "searchProvider": "string — dual | searxng | bing | google | duckduckgo"
  },

  "execution_phases": [
    "1. Internal corpus search (if discoveryInternalFirst=true) via _searchSourceCorpusFn",
    "2. Internet search via _runSearchProvidersFn (concurrent per queryConcurrency)",
    "3. Plan-only fallback (if no provider available) via buildPlanOnlyResults()"
  ],

  "output": {
    "rawResults": ["{ url, title, snippet, provider, query, ... }"],
    "searchAttempts": ["{ query, provider, result_count, reason_code, duration_ms }"],
    "searchJournal": ["{ ts, query, provider, result_count, reason_code }"],
    "internalSatisfied": "boolean — internal corpus met required field targets",
    "externalSearchReason": "string | null — why external search was run/skipped"
  }
}
