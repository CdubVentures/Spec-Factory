export function buildDiscoverySeedPlanContext({
  config,
  storage,
  category,
  categoryConfig,
  job,
  runId,
  logger,
  roundContext,
  requiredFields,
  llmContext,
  frontierDb,
  planner,
  normalizeFieldList,
} = {}) {
  return {
    config,
    storage,
    category,
    categoryConfig,
    job,
    runId,
    logger,
    roundContext,
    requiredFields,
    llmContext,
    frontierDb,
    planner,
    normalizeFieldListFn: normalizeFieldList,
  };
}
