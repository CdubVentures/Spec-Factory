// Settings feature — public API barrel.
// Consumers must import from this file, not from internal paths.

export { registerConfigRoutes } from './api/configRoutes.js';
export { createConfigRouteContext } from './api/configRouteContext.js';
