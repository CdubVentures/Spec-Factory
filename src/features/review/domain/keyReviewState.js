// Stubbed — key_review_state tables removed in Phase 1b.
// applySharedLaneState is still imported by component/enum mutation routes
// but is now a no-op since the lane state system is retired.
export function applySharedLaneState() {
  return null;
}
